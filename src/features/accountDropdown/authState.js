export function getCurrentUser() {
  // Temporary

  return null;

  /*
  Later

  return JSON.parse(
    localStorage.getItem("user")
  );

  or

  return auth.user;

  or

  fetch("/api/auth/me")
  */
}
